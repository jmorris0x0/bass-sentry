# Sets the default listener on port 1883 for all interfaces
listener 1883

# Enable anonymous access (for simplicity, but in production you'd likely want authentication)
allow_anonymous true

# Other default configurations can be included as needed.

